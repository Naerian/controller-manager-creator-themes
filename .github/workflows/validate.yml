name: Validate creator themes
on:
  pull_request:
jobs:
  validate:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - shell: pwsh
        run: |
          ./tools/validate-themes.ps1
          ./tools/test-validator.ps1
